<div class="flex flex-col justify-center items-center">
	<h1 class="mb-8 text-5xl font-extrabold font-mono"><%= t('form.create_task').camelize(:upper) %></h1>

	<%= render "form", url: { action: 'create', controller: 'tasks' } %>

  <%= link_to t('form.back_to_task_list'), root_path, class: "mt-5 border-2 px-5 py-2 rounded-lg border-black border-b-4 font-black translate-y-2 border-l-4 bg-black text-white hover:bg-mybg hover:text-black" %>
</div>